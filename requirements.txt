# Phase A — extraction
beautifulsoup4>=4.12
lxml>=5.0
pdfminer.six>=20231228
jsonschema>=4.20

# Phase C/D — classifier & retrieval (added when those phases land)
# rank-bm25>=0.2.2

# Phase E — numerical eval (optional)
# numpy>=1.26
# scipy>=1.12
# sympy>=1.12

# Phase I/J — Qwen inference & LoRA training (remote GPU mostly)
# torch>=2.2
# transformers>=4.41
# peft>=0.10
# accelerate>=0.30
# datasets>=2.18
# trl>=0.8
# bitsandbytes>=0.43

# Tooling
pytest>=8.0
